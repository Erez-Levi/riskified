apiVersion: v1
kind: Pod
metadata:
  name: app  
spec:
  containers:
  - name: app-pod
    image: {{ .Values.pod.image }}:{{ .Values.pod.tag }}
    env:
    - name: RESPONSE
      value: {{ .values.pod.response }}
